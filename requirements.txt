pymavlink>=2.4.41   # parser oficial de logs ArduPilot (.bin dataflash)
pyulog>=1.2.0        # parser oficial de logs PX4 (.ulog)
pandas>=2.2.0        # manipulacion tabular de telemetria (usado por los 3 parsers)
folium>=0.16.0        # mapas interactivos (Leaflet) para la trayectoria de vuelo
plotly>=5.20.0        # graficos interactivos de altitud/velocidad/bateria
jinja2>=3.1.0          # plantillas para el informe HTML final
srt>=3.5.3            # parseo de subtitulos SRT (telemetria embebida en video DJI)
scikit-learn>=1.4.0    # deteccion de anomalias no supervisada (Isolation Forest)
pytest>=8.0.0          # framework de tests (dependencia de desarrollo)
fastapi>=0.110.0        # interfaz web (subir log -> ver informe en el navegador)
uvicorn>=0.29.0          # servidor ASGI para correr la interfaz web
python-multipart>=0.0.9  # soporte de subida de archivos (multipart/form-data) en FastAPI
httpx>=0.27.0            # cliente HTTP usado por TestClient en los tests de la web (dependencia de desarrollo)
ruff>=0.16.0             # lint + formato de codigo (dependencia de desarrollo)
slowapi>=0.1.9           # limite de peticiones por IP en la interfaz web
mypy>=1.10.0             # comprobacion estatica de tipos (dependencia de desarrollo)
pytest-cov>=5.0.0        # cobertura de tests (dependencia de desarrollo)
